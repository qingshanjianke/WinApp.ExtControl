name: .NET Class Library CI/CD - Trusted Publishing

on:
  push:
    tags:
      - 'v*'   # 当推送以 v 开头的 tag 时触发，如 v1.0.0

permissions:
  id-token: write    # ✅ 必须，允许 GitHub 发 OIDC 令牌
  contents: read

env:
  DOTNET_VERSION: '8.0.x'
  PROJECT_PATH: './WinApp.ExtControl.csproj'
  CONFIGURATION: 'Release'
  NUGET_SOURCE: 'https://api.nuget.org/v3/index.json'

jobs:
  build-and-publish:
    runs-on: windows-latest  # ✅ 保留 Windows 构建环境
    environment: release

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: 🏗️ 设置 .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 🔍 显示环境信息
      run: |
        echo "运行器操作系统: Windows"
        dotnet --info
        dotnet --list-sdks

    - name: 🧩 还原依赖
      run: dotnet restore ${{ env.PROJECT_PATH }}

    - name: 🔨 编译项目
      run: dotnet build ${{ env.PROJECT_PATH }} --configuration ${{ env.CONFIGURATION }} --no-restore

    - name: 🧪 运行测试
      run: dotnet test ${{ env.PROJECT_PATH }} --configuration ${{ env.CONFIGURATION }} --no-build --logger "trx;LogFileName=test_results.trx"

    - name: 📦 打包 NuGet 包
      shell: pwsh
      run: |
        $VERSION = $env:GITHUB_REF_NAME -replace '^v', ''
        Write-Host "打包版本: $VERSION"
        dotnet pack $env:PROJECT_PATH `
          -p:Version=$VERSION `
          --configuration $env:CONFIGURATION `
          --output ./artifacts `
          --include-symbols --include-source `
          -p:SymbolPackageFormat=snupkg

    # 🌟 新增：使用 OIDC 登录 NuGet.org 获取临时 API Key
    - name: 🔐 认证到 NuGet.org（Trusted Publishing）
      id: login
      uses: NuGet/login@v1
      with:
        user: qingshanjianke

    # 🚀 发布包
    - name: 🚀 发布到 NuGet.org
      run: |
        echo "查找 nupkg 文件..."
        dir . -Recurse -Filter *.nupkg
        $pkg = Get-ChildItem -Recurse -Filter *.nupkg | Select-Object -First 1
        echo "找到包: $($pkg.FullName)"
        dotnet nuget push "$($pkg.FullName)" --api-key ${{ steps.login.outputs.NUGET_API_KEY }} --source ${{ env.NUGET_SOURCE }} --skip-duplicate
      shell: pwsh
      
